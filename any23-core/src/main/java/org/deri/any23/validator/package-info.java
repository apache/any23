/**
 * This package defines a simple rule based validation
 * framework able to detect ({@link org.deri.any23.validator.Rule}) and
 * fix ({@link org.deri.any23.validator.Fix}) common mistakes that
 * can be present within an <i>HTML</i> DOM model and
 * that can prevent the extraction of metadata.
 */
package org.deri.any23.validator;